name: create-react-app
region: us-east-1
yamlVersion: 2
frontend:
    path: ./
    publish: build
    scripts:
        deploy: npm install
        build: react-scripts build
    subdomain: create-react-app
